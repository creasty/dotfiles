- set_fact:
    anyenv: {}

- name: install anyenv
  homebrew:
    name:  anyenv
    state: latest

- name: initialize manifest
  command: |
    bash -lc 'anyenv install --force-init'
  environment:
    SKIP_ANYENV_INIT: true

- name: clear init script cache
  file:
    dest: '{{ item }}'
    state: absent
  with_fileglob:
    - '{{ anyenv.init_file_glob }}'
