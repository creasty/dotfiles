language:  objective-c
os:        osx
osx_image: xcode7.3  # OS X 10.11 (https://docs.travis-ci.com/user/osx-ci-environment/#OS-X-Version)

sudo: required

branches:
  only:
    - master

before_script:
  - cp ./provisioning/secrets{.sample,}.yml

script:
  - ./up
