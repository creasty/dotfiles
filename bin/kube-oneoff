#!/usr/bin/env bash

set -eu
set -o pipefail

IMAGE="${1:-koudaiii/curl}"
RUN_CMD="${2:-bash}"
NAME="creasty-oneoff-$RANDOM"

kube run \
  -it \
  --rm \
  "$NAME" \
  --image="$IMAGE" \
  --restart=Never \
  -- \
  "$RUN_CMD"
