#!/bin/bash

DEFAULT_REMOTE=origin
DEFAULT_BRANCH=$(git symbolic-ref --short HEAD)

show_and_run() {
  echo "--  $@"
  exec $@
}

if [ $# -ge 2 ]; then
  show_and_run "git pull ${1:-$DEFAULT_REMOTE} ${2:-$DEFAULT_BRANCH}"
else
  show_and_run "git pull $DEFAULT_REMOTE ${1:-$DEFAULT_BRANCH}"
fi
