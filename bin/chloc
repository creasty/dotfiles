#!/usr/bin/env bash

# Change network location setting

set -eu
set -o pipefail

scselect \
  | egrep -i "\($1" \
  | cut -c 4- \
  | cut -f 1 \
  | head -n 1 \
  | xargs scselect \
  | sed -E 's/.+ \((.+)\)/Location changed to \1/'
