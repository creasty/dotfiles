#!/usr/bin/env bash

source "$(dirname $0)/../lib/helpers.sh"

ps aux \
  | grep -v $0 \
  | grep -v grep \
  | grep -v peco \
  | sed '1d' \
  | peco --query "$@" \
  | awk '{ print $2 }' \
  | xargs kill -9
